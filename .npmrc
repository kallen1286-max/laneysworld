# Hoist all dependencies to the root node_modules so `node server/index.mjs`
# (executed from the repo root) can resolve express/cors without explicitly
# cd-ing into server/. Required because the server is a workspace package.
node-linker=hoisted
