# robots.txt for Delaney's World
# This file tells search engines how to crawl the site

# Allow all search engines to crawl the entire site
User-agent: *
Allow: /

# Sitemap location
Sitemap: https://www.laneysworld.com/sitemap.xml

# Crawl-delay (optional - helps prevent server overload)
# Most modern search engines ignore this, but it doesn't hurt
Crawl-delay: 1

# Block specific bots if needed (currently none blocked)
# Example:
# User-agent: BadBot
# Disallow: /